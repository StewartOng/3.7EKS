apiVersion: v1
kind: Service
metadata:
  name: stewart-nginx-service
  namespace: stewart-eks-activity
spec:
  type: LoadBalancer
  selector:
    app: nginx
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
